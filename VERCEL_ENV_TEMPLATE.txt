# Vercel Environment Variables Template
# Copy and paste this into Vercel's environment variables section

# Required: PostgreSQL Database URL
# You can use Vercel Postgres or any external PostgreSQL service
DATABASE_URL=postgresql://username:password@host:port/database_name?sslmode=require

# Optional: If using Vercel Postgres, it will auto-populate these
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Optional: Session Secret (will be auto-generated if not provided)
SESSION_SECRET=your-random-session-secret-at-least-32-characters

# Optional: Admin Configuration
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-admin-password

# Optional: Application Settings
NEXT_PUBLIC_APP_NAME=AI Galaxy
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Feature Flags
NEXT_PUBLIC_ENABLE_DANMAKU=true
NEXT_PUBLIC_ENABLE_FEEDBACK=true

# Build Settings (Vercel will handle these automatically)
NODE_ENV=production